#!/usr/bin/env python

import sys
import subprocess

if __name__ == '__main__':
    # Legacy wrapper kept for compatibility:
    # forward all args to the maintained processor.
    cmd = [sys.executable, "-m", "amstrax.auto_processing_new.process", *sys.argv[1:]]
    raise SystemExit(subprocess.call(cmd))
